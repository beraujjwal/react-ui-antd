/* eslint-disable @typescript-eslint/no-explicit-any */
import React from 'react';
import * as S from './AppTable.styles';

// TODO make generic!
export const AppTable = (props) => {
  return <S.Table {...props} />;
};
